mutation mutatePortfolio($portfolio: ModifyPortfolioGraph!, $messageId: Guid!, $conversationId: Guid!){
  modifyPortfolio(messageId: $messageId, conversationId: $conversationId, portfolio: $portfolio){
    id
  }
}
