query allProgramReports {
  programs(orderBy: {path: "name"}) {
    id
    name
    reports(orderBy: {path: "name"}) {
      id
      name
      __typename
      
      latestVersion
      {
          id
          notes   
      }  
    }
    __typename
  }
}