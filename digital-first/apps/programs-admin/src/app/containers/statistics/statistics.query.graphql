query allStatisticsSearch($name: String) {
  statistics(where: {path: "name", comparison: contains, value: [$name]}, orderBy:{path:"name"}) {
    id
    name
    agency {
      id
      title
    }
  }
}
