query user ($userId: String!) {
  user(id:$userId)
  {
    id
    emailAddress
    agency{
      id
      title
    }
    apiKeys{
      id
      key
      rowVersion
      disable
    }
    lastLogin
    rowVersion
  }
}
