query allGroups{
  groups(orderBy:{path:"title"}){
    id
    title
    rowVersion
  }
}

query allGroupsSearch($title: String) {
   groups(where: {path: "title", comparison: contains, value: [$title]},orderBy:{path:"title"}){
       id
       title
       rowVersion
     }
}
