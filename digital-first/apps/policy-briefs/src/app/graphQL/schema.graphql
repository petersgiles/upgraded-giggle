directive @client on FIELD

# import NavNode from "navigatorTree.graphql"

type NavigatorTreeNode {
	isActive: Boolean
    isExpanded: Boolean
}

input ActivatePackNavigationNodeInput {
	id: ID
	isActive: Boolean
}

input TogglePackNavigationNodeInput {
	id: ID
	isExpanded: Boolean
}

type Mutation {
    toggleExpandPackNavigationNode(input: TogglePackNavigationNodeInput!): Boolean
	activatePackNavigationNode(input: ActivatePackNavigationNodeInput!): Boolean
}

