schema:
  - http://localhost:3008/graphql:
      headers:
        ProgramsApiKey: "peter.giles@pmc.gov.au"
documents:
  - ./apps/commitments-reader/**/*.graphql
overwrite: true
config:
generates:
  ./apps/commitments-reader/src/app/generated/graphql.ts:
    config: { }
    plugins:
      - "typescript-common"
      - "typescript-client"
      - "typescript-apollo-angular"
require: []
