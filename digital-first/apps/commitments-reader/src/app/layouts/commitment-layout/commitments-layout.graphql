query GetRefinerTags {
  refiners {
    id
    title
    expanded
    selected
    groupId
    children {
      id
      title
      expanded
      selected
      groupId
      children {
        id
        title
        groupId
        expanded
        selected
      }
    }
  }
}
