query MapPointsSearch($refiner: CommitmentRefinerGraph!, $bookType: BookType!) {
  mapPoints(refiner: $refiner, book: $bookType) {
    id
    title
    placeId
    latitude
    longitude
    commitmentMapPoints {
      commitment {
        id
        title
        commitmentType {
          id
          title
        }
        criticalDate {
          id
          title
        }
        portfolioLookup {
          id
          title
        }
        portfolioLookupId
      }
    }
  }
}
