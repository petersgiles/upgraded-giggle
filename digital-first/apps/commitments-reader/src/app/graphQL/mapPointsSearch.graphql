query MapPointSearch(
  $refiner: CommitmentRefinerGraph!
  $bookType: String!
) {
  commitments(refiner: $refiner, bookType: $bookType) {
    id
    commitmentMapPoints {
      id
      mapPoint {
        id
        placeId
        title
        latitude
        longitude
      }
    }
  }
}
