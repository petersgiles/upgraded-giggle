query getCommitmentDetail($id: String!, $bookType: BookType!) {
  commitments(id: $id, bookType: $bookType) {
    id
    title
    description
    bookType
    cost
    date
    politicalParty
    statusId
    announcedBy
  }
}
