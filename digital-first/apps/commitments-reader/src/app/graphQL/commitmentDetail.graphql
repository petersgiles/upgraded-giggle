query getCommitmentDetail($id, id) 
{
commitments(id: $id) {
  id
  title
  description
  bookType
  cost 
  date 
  politicalParty
  statusId
  announcedBy
  announcementType { 
      id
      title 
    } 
  criticalDate {
      id
      title
  }
  portfolioLookup {
      id
      title
  }

  }