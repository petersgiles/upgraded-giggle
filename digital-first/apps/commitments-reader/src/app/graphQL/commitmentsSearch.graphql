query CommitmentsSearch($refiner: CommitmentRefinerGraph!, $bookType: BookType!) {
  commitments(refiner: $refiner, book: $bookType) {
    id
    title
    bookType: book
    politicalParty
    announcedBy
    announcementType {
      id
      title
    }
    criticalDate {
      id
      title
    }
    portfolioLookup {
      id
      title
    }
  }
}
