schema:
  - http://localhost:3002/graphql:
      headers:
        ProgramsApiKey: "peter.giles@pmc.gov.au"
documents:
  - ./apps/deck/src/**/*.graphql
overwrite: true
config:
generates:
  ./apps/deck/src/app/generated/graphql.ts:
    config: {}
    plugins:
      - "typescript-common"
      - "typescript-client"
      - "typescript-apollo-angular"
require: []
